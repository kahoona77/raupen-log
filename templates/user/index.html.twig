{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <div class="columns">
        <div class="column">
            <h1>Benutzer</h1>
        </div>
        <div class="column">
            <a class="button is-primary is-pulled-right" href="{{ path('app_user_edit', {'id': 0}) }}">Neuer Benutzer</a>
        </div>

    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Benutzername</th>
                <th class="has-text-centered">Admin</th>
                <th>Anzeigename</th>
                <th class="has-text-centered">Bearbeiten</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.username }}</td>
                <td class="has-text-centered">
                    <span class="icon is-small"><svg>
                    {% if user.admin %}
                        <use xlink:href="{{ asset('images/regular.svg#check-square') }}"></use>
                    {% else %}
                        <use xlink:href="{{ asset('images/regular.svg#square') }}"></use>
                    {% endif %}
                    </svg></span>
                </td>
                <td>{{ user.DisplayName }}</td>
                <td class="has-text-centered">
                    <a class="button is-small" href="{{ path('app_user_edit', {'id': user.id}) }}">bearbeiten</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
